# Ollama connection (server-side only — do not prefix with NEXT_PUBLIC_)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=myai:qwen3-8b

# faster-whisper-server (server-side only — do not prefix with NEXT_PUBLIC_)
WHISPER_SERVER_URL=http://127.0.0.1:8000

# voicepeak-server (server-side only — do not prefix with NEXT_PUBLIC_)
VOICEPEAK_SERVER_URL=http://127.0.0.1:8001
